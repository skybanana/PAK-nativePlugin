- 스피커/모니터 출력 영향은 제외했습니다. 출력 없이도 동일했습니다.
- aubio_onset_do() 호출 지속시간이나 onset timestamp 기준 160 ms 판정 창은 주원인이 아닙니다.
  - judgeWindowTest는 플러그인 방식이라 capture가 약 3~4 block 짧았지만, 성공률은 기존과 거의 같았습니다.
- 잔향 onset이 수집 중 다음 판정을 선점하는 현상은 단독 C5 테스트에서 거의 없었습니다.
  - detected == started, ignored while collecting == 0이 대부분이었습니다.
  - 따라서 연속 실패의 주원인으로 보기 어렵습니다.
- 플러그인에서는 AudioBlock queue 유실이 확실히 발생했습니다.
  - FingeringPractice2.log에서 droppedAudioBlocks가 0에서 211까지 증가했습니다.
  - chord FFT/pending 처리 중 judge thread가 callback 입력 속도를 따라가지 못하고, 8-block queue가 차서 입력이 버려집니다.
  - 이 유실은 플러그인이 단독 chordOnsetTest보다 나빠지는 핵심 원인입니다.
- queue 유실이 없어도 단독 테스트에서 간헐적·연속 chord fail은 남습니다.
  - 현재 코드가 각 target fundamental의 최대 FFT peak가 계산된 중심 bin과 정확히 일치할 때만 통과시키기 때문입니다.
  - 기타의 미세 튜닝, 프렛 압력, 피킹 직후 pitch bend, harmonic 비율 변화만으로 인접 bin peak가 되어 실패할 수 있습니다.
  - 특히 저음 G2/C3는 FFT bin 경계에 민감합니다.
    결론적으로 개선 우선순위는 다음입니다.

1. AudioBlock queue 유실 제거 또는 감소
2. chord fundamental의 exact-bin 판정 완화 또는 실제 peak-bin 진단 추가
3. onset window/잔향 문제는 현재 단계에서 후순위
