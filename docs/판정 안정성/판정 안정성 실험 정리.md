# 판정 안정성 실험 정리

## 목적

`PracticeModeFingeringTest`의 코드 판정이 반복 연주 후 불안정해지는 원인을 찾고,
단독 입력 테스트인 `chordOnsetTest`와의 차이를 확인한다.

## 관찰된 현상

- `FingeringPractice.log`에서는 초반에 G5/C5를 통과하다가 뒤로 갈수록 같은 코드를
  연주해도 실패가 연속 발생했다.
- `chordOnsetTest`에서는 같은 코드를 반복 입력해도 플러그인보다 높은 빈도로 통과했다.
- `FingeringPractice2.log`에서 Target 10 부근부터 chord pass는 9에서 멈추고 fail만
  증가했다.

## 실험과 결과

### 1. 스피커 출력 영향

스피커를 끈 상태에서도 같은 문제가 발생했다.

- 결론: 모니터 출력의 음향 피드백은 주원인에서 제외한다.

### 2. 판정 창 기준 시각

`chordOnsetTest`는 onset을 감지한 callback 시각부터 160 ms를 수집한다.
`judgeWindowTest`는 플러그인처럼 `aubio_onset_get_last_s() + 160 ms`를 deadline으로
사용한다.

| 테스트 | 판정 수 | 통과 | 실패 | 통과율 | 캡처 크기 |
| --- | ---: | ---: | ---: | ---: | --- |
| `chordOnsetTest` 앞 53회 | 53 | 34 | 19 | 64.2% | 61~62 block, 7808~7936 sample |
| `judgeWindowTest` | 53 | 36 | 17 | 67.9% | 58~59 block, 7424~7552 sample |

플러그인 방식이 3~4 block 짧게 캡처했지만 통과율 차이는 2회뿐이었다. 서로 다른 연주
세션의 결과이므로 이 차이를 유의미한 성능 차이로 볼 수 없다.

- 결론: onset timestamp를 deadline으로 쓰는 방식은 캡처 길이를 바꾸지만, 이번 문제의
  주원인은 아니다.

### 3. AudioBlock queue 유실

`PracticeModeFingeringTest`에 `droppedAudioBlocks`를 출력해 확인했다.
`FingeringPractice2.log`에서 이 값은 0에서 211까지 증가했다. 반면 `droppedJudgeEvents`는
0이었다.

`droppedJudgeEvents`는 Unity로 전달할 판정 이벤트 큐의 상태이고,
`droppedAudioBlocks`는 callback에서 judge thread로 전달하는 실제 입력 음원의 유실이다.
따라서 두 값은 구분해서 봐야 한다.

- 결론: chord FFT와 pending judgment 처리 중 judge thread가 입력 속도를 따라가지 못해
  8-block audio queue가 차고, 실제 입력 block이 유실된다. 이는 플러그인이 단독 테스트보다
  불안정한 확정 원인이다.

### 4. AudioBlock queue 확장

audio queue 할당 수를 8에서 64로 늘렸다. 빈 상태 구분을 위해 한 칸을 비우므로 실제 적재
가능 수는 7에서 63 block으로 증가했다.

| 테스트 | AudioBlock 유실 | chord 통과 | chord 실패 | 통과율 | 진행 위치 |
| --- | ---: | ---: | ---: | ---: | --- |
| `FingeringPractice2.log` | 211 | 9 | 162 | 5.3% | Target 10 부근 정체 |
| `FingeringPractice(queueExtend).log` | 0 | 46 | 50 | 47.9% | Target 48 |

확장 후 모든 상태 보고에서 `droppedAudioBlocks == 0`이었다. chord 통과도 Target 10 이후
계속 증가했다.

- 결론: 8-block queue 유실은 연속 실패와 진행 정체의 직접 원인이었다. 64-block queue는
  이번 실험 범위에서 이를 제거했다.

### 5. 잔향 onset 선점

`chordOnsetTest`는 수집 중에도 `aubio_onset_do()`를 계속 실행하며 다음 값을 출력한다.

- `detected`: 감지된 전체 onset 수
- `started`: 실제 chord 수집을 시작한 횟수
- `ignored while collecting`: 이미 수집 중이라 무시된 onset 수

대부분의 단독 테스트 구간에서 `detected == started`이고 `ignored while collecting == 0`이었다.
후반에 무시된 onset은 1회뿐이었다.

- 결론: 잔향 onset은 존재할 수 있으나, 단독 테스트에서 보인 연속 chord 실패의 주원인은 아니다.

### 6. C5 코드로 단독 테스트 전환

`chordOnsetTest`의 검사 대상을 G5에서 C5(C3 + G3)로 변경했다. C5에서도 간헐적 또는
연속 실패가 남았다. 즉 차트의 코드 종류만으로 플러그인과 단독 테스트의 차이를 설명할 수 없다.

현재 fundamental 판정은 각 목표 음의 탐색 범위에서 가장 큰 FFT bin이 계산된 중심 bin과
정확히 같을 때만 통과시킨다.

```cpp
if (strongestBin != centerBin)
    return false;
```

저음 fundamental은 기타의 미세한 튜닝, 프렛 압력, 피킹 직후 pitch bend, harmonic 비율 변화에
따라 peak가 인접 bin으로 이동하기 쉽다. 이 경우 주변 에너지가 충분해도 즉시 실패한다.

- 결론: AudioBlock 유실과 별개로 exact-bin 조건은 단독 테스트의 간헐적·연속 실패를 만드는
  유력한 원인이다.

### 7. 기타 노이즈 영향

64-block queue 상태에서 기타의 불필요한 소리를 제거한 뒤 같은 운지 연습을 반복했다.

| 테스트 | AudioBlock 유실 | chord 통과 | chord 실패 | 통과율 | 진행 위치 |
| --- | ---: | ---: | ---: | ---: | --- |
| `FingeringPractice(queueExtend).log` | 0 | 46 | 50 | 47.9% | Target 48 |
| `fingeringPractice(NoseRemove).log` | 0 | 36 | 10 | 78.3% | Target 37 |

노이즈 제거 후 G5 구간은 24회 시도 중 통과 23회, 실패 4회였다. 남은 실패 10회 중 5회는
C5 전환 초기에 몰렸고, 이후 C5는 연속 통과했다. 반대로 디스토션을 의도적으로 적용하면
인식률이 다시 낮아졌다.

- 결론: 기타 노이즈와 디스토션이 FFT peak 및 코드 판정에 큰 영향을 준다. exact-bin 조건을
  바로 완화하기보다, 실패 시 각 음의 `centerBin`과 `strongestBin`을 기록해 실제 원인을
  먼저 확인해야 한다.

### 8. judge 처리 속도

`judgeConsumeTest`로 callback에서 queue에 들어온 block 수와 judge thread가 처리를 끝낸
block 수를 1초 단위로 측정했다.

- 입력 및 처리 속도: 대체로 375~383 block/s로 동일
- backlog: 대부분 0, 관측 최대 4 block
- AudioBlock 유실: 전체 구간 0

현재 64-block queue의 실제 적재 가능 수는 63 block이므로, 관측된 최대 backlog는 약 6%다.
backlog는 다음 보고 구간에 0으로 회복됐다.

- 결론: 현재 측정에서는 FFT 확정 처리 때문에 judge thread가 지속적으로 입력을 따라가지
  못한다는 증거가 없다. 다만 1초 평균 측정이므로 짧은 순간 지연의 최고치는 확인할 수 없다.

## 최종 결론과 우선순위

1. **64-block AudioBlock queue를 유지한다.** 이번 실험 범위에서 유실과 진행 정체를
   제거했다.
2. **노이즈·디스토션 상태의 fundamental 판정을 진단한다.** 실패 시 각 음의 `centerBin`과
   `strongestBin`을 기록하고, 그 결과를 근거로 exact-bin 조건 완화 여부를 결정한다.
3. **FFT worker 분리는 보류한다.** 현재 처리 속도 측정에서 지속적인 backlog는 없었다.
   순간 처리 시간을 추가 측정해 필요성이 확인될 때 다시 검토한다.
4. onset timestamp 기반 판정 창과 잔향 onset 선점은 현재 증거상 후순위다.

## 관련 로그와 테스트

- `docs/FingeringPractice.log`
- `docs/FingeringPractice2.log`
- `docs/chordOnset.log`
- `docs/judgeWindowTest.log`
- `docs/판정 안정성/FingeringPractice(queueExtend).log`
- `docs/판정 안정성/fingeringPractice(NoseRemove).log`
- `docs/판정 안정성/JudgeProcessTime.log`
- `test/play/PracticeModeFingeringTest.cpp`
- `test/performanceTest/chordOnsetTest.cpp`
- `test/performanceTest/judgeWindowTest.cpp`
- `test/judgeConsumeTest.cpp`
